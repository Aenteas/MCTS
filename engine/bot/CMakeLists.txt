# This file has been generated by scripts/generate_cmake.py

cmake_minimum_required (VERSION 3.22.0)

if(USE_ENGINE_BOT_MCTS)
  message(STATUS "Configuring library at /home/david/projects/mcts_swig/engine/bot/mcts ...")
  add_subdirectory(mcts)
endif()
if(USE_ENGINE_BOT_RANDOM)
  message(STATUS "Configuring library at /home/david/projects/mcts_swig/engine/bot/random ...")
  add_subdirectory(random)
endif()
if(USE_ENGINE_BOT_SCHEDULER)
  message(STATUS "Configuring library at /home/david/projects/mcts_swig/engine/bot/scheduler ...")
  add_subdirectory(scheduler)
endif()
if(USE_ENGINE_BOT_HASHTABLE)
  message(STATUS "Configuring library at /home/david/projects/mcts_swig/engine/bot/hashtable ...")
  add_subdirectory(hashtable)
endif()
if(USE_ENGINE_BOT_BASE)
  message(STATUS "Configuring library at /home/david/projects/mcts_swig/engine/bot/base ...")
  add_subdirectory(base)
endif()